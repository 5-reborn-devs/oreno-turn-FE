using UnityEngine;

public class resultAnim : MonoBehaviour
{   

   [SerializeField] public GameObject bigSquare;
   [SerializeField] public GameObject smallquare;
   [SerializeField] public GameObject victory;
   [SerializeField] public GameObject Winner;
        
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
